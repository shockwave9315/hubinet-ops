# Hubinet Ops 0.5 — implementation status

Status date: 2026-08-28

**This document is the current implementation map for Hubinet Ops 0.5.** It
answers: what works now, what exists but is dormant, what is not implemented,
what is blocked, and what the active next product direction is.

It is **not** an architecture authority. When it conflicts with an ACCEPTED ADR,
the ADR wins and this document must be corrected.

Start at `docs/architecture/README.md` for the documentation index, the default
agent reading set, and the archive policy.

## Authority sources

| Document | Status | Read it when |
| --- | --- | --- |
| `AGENTS.md` | binding repository rules | always |
| `docs/product-intent.md` | **ACTIVE PRODUCT INTENT** — binding for product direction/priority; not an ADR, subordinate to ACCEPTED ADRs on any architecture/security invariant | always |
| `docs/architecture/0.5-foundation.md` | **ACCEPTED** (Phase 0 decisions) | architecture work |
| `docs/architecture/0.5-inventory-model.md` | **ACCEPTED** | inventory/reconciliation/HA contract work |
| `docs/architecture/adr/0001-resource-identity-incarnation.md` | **ACCEPTED** | anything touching identity, VMID, incarnation |
| `docs/architecture/adr/0002-proxmox-discovery-reconciliation.md` | **ACCEPTED** | discovery/reconciliation/presence work |
| `docs/architecture/adr/0003-source-binding-attestation.md` | **ACCEPTED** | source binding/attestation/endpoint work |
| `docs/architecture/adr/0004-confirmed-removal-operator-absence.md` | **ACCEPTED** | confirmed removal / absence proof |
| `docs/architecture/adr/0005-workload-continuity-enrollment.md` | **ACCEPTED** — negative stock-PVE trust boundary and R0 safety decision only | continuity/trust questions only |
| `docs/architecture/adr/0006-workload-continuity-stronger-proof.md` | **ACCEPTED** — negative/unresolved research record and normative requirements only | continuity/trust questions only |
| `docs/architecture/0.5-r0-read-only-runtime-activation.md` | design contract cited by name and section from the live R0 runtime code | R0 runtime, transport, scheduler, config, install work |
| `docs/architecture/research/adr0006-workload-continuity-evidence.md` | **NON-NORMATIVE** evidence supporting ADR 0006 | only when doing continuity-proof research |

ADR 0005 and ADR 0006 remain ACTIVE AUTHORITY. Neither selects a sufficient
Blocker-B mechanism, neither grants `security_continuity=trusted` anywhere, and
neither authorizes schema, runtime, `hostd`, enrollment, Home Assistant, or
mutation work. ADR 0006's classifications remain: PVE task/event history =
UNRESOLVED/NOT FULLY AUDITED; single-node `pmxcfs` = UNRESOLVED/NOT FULLY
AUDITED; distributed per-node `pmxcfs` = UNRESOLVED/NOT AUDITED HERE; broader
host-rooted/external mechanisms = UNRESOLVED; D/E/F-narrow = No/not sufficient;
C = not sufficient/not applicable.

"ACTIVE AUTHORITY" does not mean every agent must read the file for every task.
See `docs/architecture/README.md` for the task-to-document matrix.

The full acceptance/review/dogfood narrative behind everything below is
preserved verbatim in
`docs/archive/project-history/0.5-r0-activation-chronology.md`. Do not read it
for ordinary implementation work.

## Current state in one table

| Item | Current result |
| --- | --- |
| R0 read-only runtime | **IMPLEMENTED, MERGED, DEPLOYED** |
| R0 operational decision | **GO** |
| R0 mode | **STRICTLY READ-ONLY** |
| mutation authority | **NONE** |
| Home Assistant enrollment/acceptance | **PASS** |
| package/update scanning | **NOT IMPLEMENTED** (next product direction) |
| jobs / snapshots / update execution | **NOT IMPLEMENTED** |
| source attestation authority (WAVE C1) | implemented, **dormant** |
| confirmed-removal authority (WAVE A1) | implemented, **dormant** |
| Blocker B (persistent workload-incarnation proof) | **OPEN** |
| Family B / B-S1 task-witness research | **ABANDONED as an implementation path** — see the postmortem |
| WAVE B1 (durable trusted enrollment) | **DEFERRED / NOT AUTHORIZED** |

## Current implementation baseline / phase

Current 0.5 implementation baseline after merged Phase 1A/1B and the merged WAVE R0-B read-only runtime activation:

- merged Phase 0 baseline: `74d9276559412b6b6a9266c865dffc11d9a7a81f`;
- PR #26 closed the deterministic/property contract-hardening gate;
- PR #35 (merge commit `20c0484d1efffcf1fe700ac1cd232cecba4fc0ea`) merged WAVE R0-B, activating the Phase 1A/1B read-only discovery/reconciliation/publication path in the repository's own production composition root;
- PR #38 merged the WAVE R0-C automated Proxmox bootstrap; dogfood #3, from commit `3d6d0865b28c5c6070cb565ff5b7af49bb7147d2`, achieved a fresh, clean Phase 1-13 bootstrap PASS on a real host;
- docs/process-only changes do not advance this implementation baseline unless they change the implemented runtime/contract phase;
- current phase: **Phase 0 is closed; the Phase 1A/1B read-only inventory backend is implemented, tested, runtime-activated (read-only), deployed on a real host, and operationally accepted with decision GO.** Source-attestation (WAVE C1) and confirmed-removal (WAVE A1) authority remain dormant, because R0's own call surface never invokes either;
- 0.5 remains a clean break from 0.4. No migration/compatibility layer is part of the 0.5 design.

The Phase 0 snapshot is intentionally a backend-owned authoritative published view consumed by Home Assistant. HA validates and presents it; HA does not become an inventory reconciler or mutation authority.

## COMPLETE

The following are implemented on `main` and covered by repository/CI tests.

### Canonical Phase 0 snapshot contract

- backend identity uses canonical `backend_instance_id` UUID;
- inventory sources use backend-generated `inventory_source_id` UUIDs;
- resources use opaque backend-generated `resource_id` UUIDs;
- VMID is a source-local locator slot, never durable resource identity;
- `resource_type` is an immutable occupant property;
- active binding identity, locator generation, and `resource_continuity_revision` are distinct concepts;
- source/node/resource references use canonical lower-case hyphenated non-NIL UUID text where the contract requires UUID identity;
- unsupported enum values and malformed security-sensitive canonical values fail closed;
- published mappings/sequences are recursively frozen or rejected when outside the JSON-like contract;
- top-level snapshot collections are detached from caller-owned mutable lists.

### Structural Phase 0 contract layer

- the canonical Phase 0 contract is split into explicit enum, model, primitive,
  source/resource/snapshot/transition validation, and projection modules under
  `custom_components/hubinet_ops/contract/`;
- `custom_components/hubinet_ops/api.py` is a thin API/client/transport
  compatibility facade;
- existing production imports through `.api` remain supported through exact
  object-identity re-exports;
- the split has zero intended semantic change and retains the existing
  accepted/rejected snapshot contract tests.

### Resource state and retained identity

Canonical resource state is split into independent axes:

- presence;
- lifecycle;
- observational continuity;
- security continuity;
- detail status;
- node availability;
- state level.

Implemented constraints include:

- at most one current/active occupant for one `(inventory_source_id, vmid)` slot;
- retained terminal/non-current incarnations coexist with a current successor under distinct `resource_id` values;
- retained locator generations are unique and consecutive within retained history, while retained history need not begin at generation 1;
- current reuse of a retained locator must use the exact next generation;
- active binding IDs cannot be reassigned to another resource after observed ownership/closure;
- terminal resources cannot reopen or silently change immutable identity;
- direct replacement retains old lineage and publishes a distinct successor;
- successor lineage is historical provenance, not a permanent requirement that the successor remain in its initial live state;
- source/node/resource records previously published cannot disappear from a later accepted Phase 0 snapshot;
- node/resource ownership cannot move across inventory sources under the same retained identity;
- same-source rename/runtime/node-presentation changes preserve resource identity;
- terminal and absent resources remain retained but their HA entities are unavailable as required by the accepted presentation contract.

### Security continuity, state level, policy and capabilities

Implemented fail-closed presentation/contract checks include:

- `discovered`, `observed`, `managed`, `maintenance`, and `break_glass` remain semantically distinct;
- normal `policy_applicable=True` is accepted only for otherwise eligible `MANAGED` or `MAINTENANCE` resources;
- `BREAK_GLASS` does not use the normal policy-applicability path;
- `policy_applicable` must be an actual runtime `bool`; malformed truthy values such as strings/integers are rejected;
- non-applicable policy cannot publish effective capabilities;
- effective capabilities are presentation facts from the backend, never frontend-derived authority;
- observed security/continuity/lifecycle/state-level transitions that affect mutation eligibility require a strictly newer `resource_continuity_revision`;
- no `+1` adjacency is required for continuity revisions;
- previously observed trusted/revoked security lower bounds are preserved according to the accepted polling contract;
- `UNVERIFIED -> REVOKED` remains legal because HA may have skipped an intermediate trust/revocation publication.

### Source health, freshness and run provenance

Phase 0 publishes and validates:

- current source health/freshness and origin/reason;
- issued/completed/applied-health/committed run sequence maxima;
- completion and applied-health outcomes;
- fixed last-success/freshness timestamps/deadline;
- exact current and committed source/endpoint/canonicalization/transport-trust context;
- monotonic source config/trust/run provenance visible to HA.

Implemented invariants include:

- `last_committed_run_sequence <= last_health_run_sequence <= latest_completed_run_sequence <= last_issued_run_sequence`;
- health provenance requires completion provenance;
- every published successful applied-health outcome names the exact committed run;
- every published successful completion outcome names the exact committed run;
- an exact committed run carries successful applied-health/completion provenance when those maxima point to it;
- a fresh source requires healthy authoritative discovery-run provenance bound to the exact committed run and committed context;
- `TIME_EXPIRY` is stale and remains fenced to the exact committed run/context;
- reversing an observed expiry back to fresh requires a newer authoritative commit;
- same-token completed/health outcomes and committed success provenance are immutable across accepted snapshots;
- optional durable maxima cannot disappear back to `None` after HA has observed them;
- a newly committed run cannot be retroactively selected from the range of runs a previous accepted snapshot had already observed as finalized;
- polling gaps remain legal: no run-sequence or published-revision adjacency is inferred;
- every successful authoritative inventory reconciliation commit advances `inventory_revision`, including commits with unchanged resource facts;
- other API-visible derived/source-health transitions may advance only `published_state_revision` when the accepted inventory projection is unchanged.

### Source context and endpoint presentation contract

Implemented Phase 0 validation includes:

- provider kind is immutable for an existing source identity;
- current endpoint identity cannot silently change for an existing source;
- endpoint identities cannot be shared across different source identities in one published namespace;
- canonical locator is immutable for the same endpoint/canonicalization contract version;
- controlled canonicalization-version migration may change the canonical locator only under the accepted higher-version/config-revision rules;
- source config/trust revisions remain monotonic without being treated as source identity or physical source-continuity proof.

This is presentation/contract validation only. It is not the Phase 1 endpoint activation/source-binding procedure.

### Home Assistant integration boundary

Implemented and tested:

- one coordinator consumes one logical backend snapshot per refresh;
- backend instance identity binds the config entry;
- foreign backend identity is rejected before publication/registry callbacks;
- resource Device/Entity Registry identity is based on backend/resource UUIDs, never VMID/name/node;
- source -> node -> resource topology is represented through Device Registry parent relations;
- rename and node migration preserve resource identity;
- unresolved/last-known node relations fail closed without inventing mutation routing;
- source freshness, resource detail availability, node availability and resource presence are separate availability gates;
- coordinator does not synthesize `missing` by diffing refreshes;
- config flow reauth/reconfigure preserves backend identity checks;
- diagnostics recursively redact repository secret classes, including camelCase names and namespaced `*_headers` containers, with negative over-redaction controls.

### Test and CI baseline

Current mandatory CI contains:

- full repository pytest on Linux;
- compileall;
- shell syntax validation;
- YAML validation;
- tracked-runtime-file validation;
- isolated Home Assistant integration tests pinned to `homeassistant==2026.8.1` on Python 3.14.x.

(The deployment smoke sandbox and managed-executor-compilation CI steps were
retired along with the legacy 0.4 deployment/managed-executor surfaces they
validated; see `tests/test_no_legacy_runtime_surface.py` and
`.github/workflows/ci.yml`.)

PR #22 final exact-head/merge-ref CI passed with:

- full repository: `1289 passed, 1 skipped`;
- HA 2026.8.1 integration suite: `150 passed`.

Native Windows execution of the exact Home Assistant pytest harness is not a compatibility gate because Home Assistant imports POSIX `fcntl` before collection. Do not patch Home Assistant, Python, or install a fake `fcntl` to make native Windows appear supported. Linux CI is the exact HA compatibility gate.

### Phase 1A persistent authority core — implemented and runtime-activated (read-only)

`app.inventory` is instantiated both by isolated tests against temporary
0.5 authority databases and, since merged WAVE R0-B, by the R0 production
composition root (`app/inventory_runtime.py`) against a real authority
database path. Application production startup
(the R0 composition root) does construct it; the production
`ProxmoxHttpTransport` and R0 scheduler use it for real Proxmox I/O; the
R0 read-only HTTP API (`GET /r0/v1/health`/`/backend`/`/snapshot`)
exposes it; Home Assistant's `HttpHubinetOpsTransport` consumes it
through that HTTP API; the R0Scheduler background thread invokes it on a
timer. No mutation eligibility or authorization depends on it (none
exists for R0), and legacy 0.4 services neither depend on it nor dual-
write/fallback to it — the legacy `app.main`/`OpsService`/`Database`
runtime is untouched and structurally excluded from the R0 composition
root's import graph. All 19 mandatory prerequisites in the Phase 1
runtime activation gate below were satisfied before WAVE R0-B wired this
subsystem in. "Runtime-activated" means implemented and constructible in
the repository — a milestone deliberately kept distinct from real-host
deployment, which happened later and separately. **Both milestones have
since been reached**: this subsystem is now deployed and running on a
real Proxmox host, with the R0 operational decision **GO** (strictly
read-only). See "Current R0 operational status" below.

- `app.inventory` is an independently instantiable 0.5 authority namespace;
  legacy `OpsService` and legacy `Database` are not owners or dependencies;
- a fresh SQLite authority database uses explicit marker
  `hubinet_ops_0_5_authority`, monotonic schema version `5`, foreign keys, WAL,
  busy timeout, and `BEGIN IMMEDIATE` write transactions; legacy 0.4, dormant
  0.5 schema v1/v2/v3/v4, and unknown non-empty databases are rejected
  without migration or reset (schema v5 is the current version; see
  "Source-binding/attestation durable authority" and "Confirmed removal
  durable authority" below for what WAVE C1 and WAVE A1 added on top of
  v3/v4, each without an in-place migration);
- one persistent backend instance owns a stable canonical UUID plus global
  `inventory_revision` and `published_state_revision` initialized to `0` and
  `1` respectively;
- atomic initial source creation persists one backend-generated source UUID,
  one initial active endpoint, one initial non-fresh runtime-health record,
  source configuration revision `1`, transport trust revision `1`, and only a
  credential reference;
- canonicalization contract version `1` supports the accepted HTTPS DNS/IPv6,
  default-port, explicit non-default-port, and root-path normalization while
  rejecting unsupported or ambiguous locators;
- source display rename preserves identity, provider kind, configuration
  revision, endpoint, and active run context while advancing both global
  revisions;
- durable discovery-run issuance captures exact source/endpoint/
  canonicalization/trust context, allocates strictly increasing per-source
  sequence values, establishes one active owner, and advances only the global
  published revision;
- exact issued/running ownership survives restart; same-source concurrent
  issuance has one winner, different sources may own runs concurrently, and a
  controlled one-time abandon fence releases ownership without invented
  observation, completion, health, or commit provenance.

### Phase 1B read-only inventory backend — implemented and runtime-activated (read-only)

Phase 1B extends only the independently instantiated `app.inventory` subsystem.
Since merged WAVE R0-B it is present in application (R0) startup,
production provider invocation, the R0 scheduler, and R0's own HTTP/HA
wiring; it remains absent from mutation authority and legacy 0.4
dependencies (neither exists for R0).

- provider contract v1 is an injected, GET-only PVE 9.x contract with an exact
  baseline/detail endpoint matrix, upstream effective-permission comparison,
  ACL/permission boundary hashing, cluster baseline, and explicit standalone
  QEMU+LXC fallback;
- immutable normalized discovery snapshots reject wrong source/context,
  malformed facts, unsupported type, duplicate locator, duplicate node, and
  incomplete security evidence before reconciliation;
- successful run finalization is one `BEGIN IMMEDIATE` transaction covering
  exact owner/context CAS, durable completion, reconciliation, nodes/resources/
  bindings, source health, committed provenance, revisions, and owner release;
- non-success completion is write-once, never changes inventory, and cannot
  overwrite health from a newer context;
- schema v5 (originally introduced as v3 for this Phase 1B inventory shape;
  WAVE C1 extended it to v4 for source attestation; WAVE A1 extended it to
  v5 for confirmed removal, see below) durably owns nodes, resource
  incarnations, locator bindings, consecutive source-local generations,
  retained terminal history/lineage, and canonicalization migration
  provenance; schema v1/v2/v3/v4 are rejected rather than migrated, per
  the clean-break design's own no-migration contract — this holds
  regardless of runtime-activation status;
- reconciliation preserves identity across facts/name/node changes, derives
  `missing` only from a complete boundary-valid baseline, quarantines gaps,
  performs atomic positive QEMU/LXC direct replacement, and never itself
  produces polling-only `confirmed_removed` — that terminal transition is
  owned exclusively by the separate, explicit Class-C authority operation
  (WAVE A1, below), never by reconciliation;
- discovery never grants trust, management, maintenance, policy applicability,
  or destructive capability;
- freshness uses observation time as anchor, materializes stale-on-arrival and
  time expiry durably, and uses run/deadline/context CAS against old workers and
  timers;
- the canonical publication assembler materializes due expiry and reads the
  backend, sources, nodes, resources, and both revisions in one SQLite read
  transaction; serialized output is accepted by the Phase 0 snapshot oracle.

**G8 (standalone/cluster `baseline_mode` ambiguity) is resolved against the
accepted model, with regression coverage.** `baseline_mode` is per-run
observed provenance recorded on each completed `discovery_runs` record
(`InventorySource` has no durable mode field); reconciliation keys
replacement only off a `resource_type` change for the same
`(inventory_source_id, vmid)` slot and never inspects `baseline_mode`.
`tests/test_inventory_reconciliation.py` locks, for both transition
directions, that a legitimate standalone↔cluster transition alone never
changes `inventory_source_id`, never bumps `source_config_revision`, and
never replaces/quarantines/marks-missing the same VMID/resource_type
incarnation, while node/topology facts may still legitimately change with
observed topology. `tests/test_inventory_discovery_snapshot.py` adds a
malformed-boundary witness (mismatched provider node-scope mode vs.
declared `baseline_mode` fails closed at snapshot construction).
`tests/test_inventory_provider_contract.py::test_caller_expectation_cannot_override_observed_mode`
already covered the provider-layer fail-closed guard against a caller
overriding the observed mode; no production behavior changed.

#### Phase 1 runtime activation gate ledger

All entries below describe backend prerequisite evidence that was
required before, and satisfied ahead of, the explicit R0 cutover review.
That review (WAVE R0-A design + WAVE R0-B implementation, two
independent reviews, Linux/HA CI) is now complete and merged (PR #35);
the read-only portion of this subsystem is runtime-activated in the
repository as described above.

| # | Status | Evidence |
| --- | --- | --- |
| 1 | DONE | ADR 0001/0002 remain ACCEPTED and their identity/security rules are preserved. |
| 2 | DONE | Existing deterministic and property Phase 0 corpus remains the publication oracle. |
| 3 | DONE | `provider.py`, provider tests, and ADR 0002 define PVE 9.x plus exact endpoint/ACL and cluster/standalone contracts. |
| 4 | DONE | Normalized completeness/detail classes isolate complete, partial, configuration error, unavailable, and invalid outcomes. |
| 5 | DONE | Phase 1A atomic source+endpoint+health transaction remains covered. |
| 6 | DONE | Canonicalization v1 and ambiguous numeric-host regressions remain covered. |
| 7 | DONE | Success finalization rechecks endpoint, locator, canonicalization version, and trust revision. |
| 8 | DONE | Typed credential/freshness transitions increment config revision and serialize/fence active ownership. |
| 9 | DONE | Atomic retained-namespace migration preserves endpoint identity and rejects alias/cross-version collision before commit. |
| 10 | DONE | Schema/reconciler own incarnation, binding, generation, retained terminal lineage, missing/gap, and direct replacement. |
| 11 | DONE | Issued/running/completed/abandoned lifecycle has exact one-time owner release and late-worker fencing. |
| 12 | DONE | Deeply immutable normalized snapshots reject malformed, duplicate, or foreign input. |
| 13 | DONE | Dormant backend publication is accepted by the existing Phase 0 contract; no HA wiring was added. |
| 14 | DONE | Reconciliation preserves present on detail failure and emits `not_applicable` for absent/terminal facts. |
| 15 | DONE | Durable success/failure health, observation-anchored deadline, expiry, stale-on-arrival, and CAS are implemented. |
| 16 | DONE | Successful reconciliation/finalization is all-or-nothing with rollback injection coverage. |
| 17 | DONE | Config/trust/canonicalization transitions serialize with active run ownership and publish controlled staleness. |
| 18 | DONE | SQLite serializes distinct publication revisions; inventory revision changes only for inventory-owned commits. |
| 19 | DONE | Publication materializes due expiry and assembles one complete view in one consistent SQLite read transaction. |

Backend prerequisites for the base read-only runtime activation gate are
complete, and the explicit activation/cutover review (WAVE R0-A/R0-B) has
since happened and merged. The read-only path is runtime-activated in the
repository, deployed on a real host, and operationally accepted (**GO**,
strictly read-only). The full dogfood/checkpoint history is in
`docs/archive/project-history/0.5-r0-activation-chronology.md`.

Of the three later capability-specific gates:

- **Blocker C** (source-binding/attestation): accepted architecture (ADR 0003)
  plus a durable-but-dormant implementation (WAVE C1, below). Endpoint
  activation, candidate promotion, and failover remain a separate,
  still-unauthorized future ADR (ADR 0003 §15).
- **Blocker A** (authoritative interval-wide absence proof): accepted
  architecture (ADR 0004) plus a durable-but-dormant minimal Class-C
  implementation (WAVE A1, below). Class A and Class B removal-authority proof
  remain entirely unresolved (ADR 0004 §26).
- **Blocker B** (workload continuity/enrollment proof): accepted architecture
  **only as a negative boundary**. Unlike Blocker A/C, ADR 0005's acceptance
  does not staging-enable an implementation wave. ADR 0005 concludes that no
  evidence candidate composed of ordinary, copyable PVE/guest/config state is
  sufficient to grant `security_continuity=trusted` under the current stock-PVE
  baseline; ADR 0006 records the stronger-proof research as
  UNRESOLVED and selects no mechanism. No schema, persistence, or runtime
  implementation exists or is authorized. WAVE B1 is **deferred, not
  authorized**.

### Source-binding/attestation durable authority (WAVE C1) — implemented but dormant

WAVE C1 extends only the independently instantiated `app.inventory`
subsystem, on schema version **4** (the version-3 marker/objects from
Phase 1A/1B are rejected without migration, exactly like v1/v2 before them;
there is no in-place v3→v4 data migration policy and none was authorized).
The durable schema/tables this wave added live in the same schema-v5
authority database the merged R0 composition root now opens at startup,
but no attestation-shaped method (`enroll_source_attestation`,
`reattest_source`, `accept_source_attestation_anchor_change`,
`revoke_source_attestation`, `check_candidate_attestation`) is ever
called by R0's own call surface — enforced by an adversarial regression
test (`tests/test_inventory_scheduler.py`) that spies every such method
across a full discovery cycle and asserts zero calls. Attestation remains
functionally dormant: no HA/MQTT wiring exposes it, and mutation
authority does not exist for R0 at all.

- `source_attestation_state`: one durable current-state row per source,
  starting explicitly `not_yet_attested` at `source_attestation_epoch = 0`;
  a monotonic `source_attestation_epoch` token, independent of
  `source_config_revision`/`transport_trust_revision`/
  `resource_continuity_revision`, gates every attestation-relevant
  security-context change; a schema trigger fails closed if
  `attestation_status`/enrolled-anchor fields ever changed without the
  epoch also advancing in the same write, so the epoch reliably proxies
  "the security context changed" for every CAS check that relies on it;
- `source_attestation_events`: an immutable, retained (delete-blocked)
  audit trail of every explicit attestation attempt/outcome
  (match/mismatch/unavailable/malformed/stale_cas/accepted/rejected),
  structurally separate from current state — current authority is never
  derived from "the latest audit event";
- explicit, operator-driven `InventoryAuthority` methods implement ADR
  0003 §12/§16/§19a/§20 literally: `enroll_source_attestation` (initial
  enrollment, `not_yet_attested`/N → `attested`/N+1 — N is normally 0, but
  a source re-enrolling after revocation carries its epoch forward rather
  than resetting), `reattest_source` (same-anchor reconfirmation is
  audit-only; a differing anchor is recorded as a mismatch and never
  silently accepted), `accept_source_attestation_anchor_change` (the
  separate explicit decision that deliberately accepts a new anchor,
  exact N→N+1), and `revoke_source_attestation` (explicit reset,
  `attested`/N → `not_yet_attested`/N+1, no remote evidence read). Every
  remote-evidence-consuming operation follows the §19a three-phase
  pattern literally: a short read-only phase-1 capture of the exact
  expected context, the trusted-reader call entirely outside any DB write
  transaction, and a phase-3 write transaction that re-validates every
  captured field by CAS before accepting any transition — a context
  change in between is audited `stale_cas` and accepts nothing;
- a durable `SourceAttestationRelationshipGate` (`clear` /
  `mismatch_pending_reattestation`, ADR 0003 §17), deliberately separate
  from `attestation_status` so a mismatch is never conflated with
  enrollment state: a mismatch (source-level or candidate-level) sets the
  gate pending without touching epoch/anchor; only an accepted anchor
  change or an explicit revocation clears it; a same-anchor
  reconfirmation never clears an already-pending mismatch; the gate never
  gates ordinary read-only discovery;
- `SourceAttestationEvidenceReader` (`app/inventory/attestation.py`) is the
  sole trusted boundary for remote evidence, injected everywhere it is
  needed; no authority method accepts a raw caller-supplied
  `tier2_verified` boolean or any other unverified evidence value. Tier 1
  (asserted PVE root-CA fingerprint, self-reported API data) and tier 2
  (cryptographic TLS-peer-chain-to-enrolled-CA verification, only ever
  produced by the trusted reader) are recorded as structurally distinct,
  durable classes (`AttestationEvidenceTier` +
  `TierTwoEvaluationStatus.not_evaluated/failed/verified`); a tier-1 match
  never becomes tier 2 by association, and a failed/unevaluated tier-2
  result never erases an independently valid tier-1 acceptance. **No
  production PVE network/TLS reader implementation exists** — the
  boundary is real and typed, but every test supplies a deterministic
  fake; concrete cryptographic chain verification against real PVE
  endpoints remains future runtime-integration work, deliberately kept
  outside this dormant wave;
- discovery-run issuance/completion/freshness provenance is fenced by
  `source_attestation_epoch` exactly like every other expected-context
  field ADR 0002 already defines (`source_config_revision`, endpoint
  identity/locator/canonicalization version, `transport_trust_revision`):
  `NormalizedDiscoverySnapshot.expected_source_attestation_epoch` must
  match the issuing run's own captured epoch; a successful completion's
  CAS additionally requires the run's expected epoch to equal the
  *current* durable epoch (read from `source_attestation_state`, never
  audit history) before reconciling anything; a failed run's health
  application uses the identical CAS, so a stale-epoch failure can never
  overwrite current health authority; every successful applicable
  completion records `committed_source_attestation_epoch`, and current
  freshness (`source_is_fresh_for_future_mutation`, expiry materialization)
  requires it to exactly equal the current epoch, alongside the existing
  committed-context fields. `attestation_status` and `relationship_gate`
  never gate ordinary read-only discovery — `not_yet_attested`/epoch 0 and
  a pending mismatch both remain fully legal, indefinitely, for base
  discovery;
- `candidate_attestation_bindings`: epoch-scoped, retained prerequisite
  evidence for a possible future endpoint-activation/failover ADR (ADR
  0003 §14/§15) — **necessary, never sufficient**, and this wave
  implements no activation, promotion, active-endpoint replacement, or
  failover of any kind. `check_candidate_attestation` requires the source
  to already be `attested` with `relationship_gate = clear`, requires the
  target endpoint to belong to the exact source and be in exact
  `lifecycle = 'candidate'` admissibility, and follows the identical
  §19a three-phase pattern against the *candidate's* own endpoint/locator
  (never the active endpoint's) — a candidate retired or otherwise made
  ineligible while the reader is in flight is caught by the same CAS and
  produces no binding. A successful match persists one immutable binding
  row scoped to the exact source/endpoint/epoch/evidence-tier/
  candidate-lifecycle/locator/canonicalization-version/transport-trust
  context, atomically with its originating event, and changes nothing
  else — no epoch bump, no anchor change, no health/freshness write, no
  resource reconciliation, no active-endpoint change, no discovery-run
  fencing. Two schema triggers add defense-in-depth beyond the
  application-layer CAS: the originating event must match the binding's
  full duplicated provenance field-for-field (not merely "some accepted
  candidate_check event exists"), and a new binding may only be inserted
  while the source's *live* current state is itself eligible under the
  exact binding epoch — both are insert-time-only checks; neither
  retroactively invalidates an already-retained binding after the source's
  epoch later advances. A binding is authority-ineligible once the live
  epoch moves past the epoch it was scoped to, but it remains physically
  retained, unmodified, and readable as historical evidence — no purge/
  retention policy exists or is authorized (ADR 0003 leaves that
  unresolved).

**Still not implemented by WAVE C1** (unchanged from before this wave):
no production PVE attestation/network reader; no startup or other runtime
activation of any kind; no HTTP/HA/MQTT exposure; no scheduler or
background attestation of any kind; no candidate-endpoint
registration/creation UX or API (tests establish a pre-existing candidate
via the same narrow raw-SQL fixture already used elsewhere in this
repository's test suite, never a promotion workflow); no
candidate→active transition; no active-endpoint replacement; no automatic
or operator-triggered failover; no implementation of the future
activation/failover ADR that §15 requires before any of that; no Blocker A
(authoritative absence proof); no Blocker B (workload/resource continuity
proof — resources discovered under an attested source remain exactly as
`unverified`/`discovered` as before this wave); no workload or resource
mutation authority of any kind. A candidate attestation binding is
retained prerequisite evidence, **not** activation authorization, and
source attestation — at either tier — proves neither physical PVE host
uniqueness nor possession of the PVE root CA private key (ADR 0003 §9,
§10, §10a, §11); this wave implements no production cryptographic
verifier, only the typed boundary a future one would plug into.

The sequence after this wave's review/merge has since progressed to WAVE
A1 (merged) and WAVE B0 (architecture corrected and **ACCEPTED** as the
negative stock-PVE trust boundary/R0 safety decision); WAVE B1 is
deferred/not authorized pending a future stronger continuity-proof ADR
(ADR 0005 §26). The current sequence is **WAVE B0 (ACCEPTED) → R0
(read-only runtime activation, DONE — merged via PR #35)**, with
operational activation on a real host at **GO** (>=24-hour window
COMPLETE; all five PR #47 observations passed on 2026-08-23),
and mutation/Phase 1C work remaining separate and gated
on its own later review after that and after a future Blocker B
stronger-proof ADR.

### Confirmed removal durable authority (WAVE A1) — implemented but dormant

WAVE A1 extends only the independently instantiated `app.inventory`
subsystem, on schema version **5** (the version-4 marker/objects from
WAVE C1 are rejected without migration, exactly like v1/v2/v3 before
them; there is no in-place v4→v5 data migration policy and none was
authorized). The durable schema/tables this wave added live in the same
schema-v5 authority database the merged R0 composition root now opens at
startup, but `InventoryAuthority.confirm_class_c_resource_removal` is
never called by R0's own call surface — enforced by the same adversarial
spy-based regression test referenced under WAVE C1 above, which also
covers this method. Confirmed removal remains functionally dormant: no
HA/MQTT wiring exposes it, and mutation authority does not exist for R0
at all.

- `resource_absence_pointers`: current sampled-absence provenance (ADR
  0004 §12 retention shape A) — one bounded, durable, mutable-current row
  per currently-`missing` resource, identifying the exact successful
  complete-baseline discovery run that most recently sampled that
  resource's slot absent. Overwritten (never appended) on each
  reconfirming run; deleted once the slot becomes present again, once a
  replaced resource retires, or once its witness is consumed by an
  accepted Class-C decision. Never immutable and never authority by
  itself — a schema trigger requires the referenced run to be a
  successful, complete-baseline, exact-same-source run (or, for the one
  legitimate in-flight case, the exact run currently being finalized by
  the very same reconciliation transaction), making it schema-impossible
  to attach a pointer to a foreign source's run even via direct SQL;
- `resource_removal_authorities` / `resource_absence_attestations`: two
  structurally separate immutable evidence tables implementing ADR 0004's
  two mandatory operator artifacts — Class-C positive removal authority
  (§9: "I am confirming removal of this exact retained resource
  incarnation") and the operator authoritative-absence attestation (§10:
  "this exact old incarnation is no longer the current occupant, and I am
  explicitly authorizing terminal closure on that administrative basis").
  Neither is ever merged into the other or inferred from it; both share
  one backend-generated decision id, and a schema trigger requires the
  attestation row to match its removal-authority row field-for-field
  (identical discipline to WAVE C1's candidate-binding-provenance
  trigger). Both tables are update-blocked and delete-blocked;
- `resource_terminations` (the existing tombstone table, reused as the
  single terminal/tombstone owner — no second tombstone concept was
  introduced) gained a nullable `class_c_decision_id` linkage column and a
  CHECK requiring exactly one of the existing `reason='replaced'` shape
  (non-null successor, null decision link) or the new
  `reason='confirmed_removed'` shape (null successor, non-null decision
  link, and a trigger requiring the linked evidence to match
  resource/binding/generation/run-sequence exactly). `run_sequence`
  continues to mean observation provenance only (ADR 0004 §20) — for
  `confirmed_removed` it is the sampled-absence witness run, never the
  timing of the Class-C decision, which instead lives on the linked
  evidence records' own `actor`/`decided_at`;
- `InventoryAuthority.confirm_class_c_resource_removal`: the explicit,
  dormant, operator-driven authority operation implementing ADR 0004's
  full atomic terminal decision (§9-§24). No remote I/O. Requires two
  independently, strictly-typed boolean assertions
  (`confirms_removal`/`attests_absence`, both must be exactly `True` --
  never a truthy string/int, never inferred from each other) plus exact
  targeting by `resource_id`/`binding_id`/`vmid`/`locator_generation`/
  `resource_continuity_revision`/witness run — there is no VMID-only entry
  point. One `BEGIN IMMEDIATE` transaction re-validates by exact CAS: no
  active discovery owner; source `attested` with `relationship_gate=
  clear`; the resource `missing`/`quarantined` with its exact binding/
  generation/revision still current and not already terminal; the current
  sampled-absence pointer references a witness run that is a genuinely
  successful, complete, boundary-consistent, available baseline, whose
  sequence exactly equals the source's *current* `last_committed_run_
  sequence` and whose committed `source_attestation_epoch` exactly equals
  the source's current epoch; and current source health/freshness/
  committed-context equality (reusing the same expiry-materialization-
  first pattern as `source_is_fresh_for_future_mutation`). Any mismatch
  writes nothing. On success: both evidence records persist; the active
  binding closes with `closure_reason='confirmed_removed'` and
  `valid_to_run_sequence` set to the *witness run's* sequence (observation
  provenance only); the resource reaches the full terminal state ADR
  0001's canonical matrix requires (`presence=confirmed_removed`,
  `lifecycle=retired`, `trusted -> revoked`, preserved last meaningful
  `observational_continuity`, preserved `last_known_node_id`); the
  tombstone is written linked to the decision; each of
  `resource_continuity_revision`/`inventory_revision`/
  `published_state_revision` advances exactly once; every source sequence/
  revision/health field is left completely untouched (no discovery run is
  fabricated); the consumed pointer is deleted; no successor is created.
  Later reappearance of the same slot (even with identical type/name/
  config/node) is handled entirely by the existing, unmodified
  unknown-VMID reconciliation path (ADR 0001/0002) — no new
  reconciliation branch was needed — and always produces a brand-new
  `resource_id`/`locator_generation`/`binding_id` with no inherited trust,
  evidence, or policy of any kind.

**Still not implemented by WAVE A1** (unchanged from before this wave): no
production runtime/HTTP/HA/MQTT/scheduler wiring of `confirm_class_c_
resource_removal` specifically (the merged WAVE R0-B read-only runtime
wires only the discovery/reconciliation/publication path described
above, and never calls this method — see "Confirmed removal durable
authority (WAVE A1)" above); no
mutation/job/policy/approval/lock framework; no candidate endpoint
activation or failover; Class A (backend-mediated removal) — still no
durable typed mutation/job authority to build it on, and still no
accepted absence-side design of its own; Class B (trusted event/cursor
proof) — the underlying contiguous-retained-event-stream contract for
stock PVE remains **UNKNOWN** and unproven; Blocker B (workload/resource
continuity proof) — entirely untouched by this wave; resources remain
exactly as `unverified`/`discovered` as before this wave, and a
confirmed-removed resource's successor starts with no inherited trust of
any kind (ADR 0004 §25). This wave implements no production Proxmox
network reader of any kind and performs no production Proxmox I/O.

Blocker B's architecture (WAVE B0) has since been corrected to an honest
negative conclusion for the current stock-PVE baseline (no evidence
candidate composed of ordinary, copyable PVE/guest/config state is
sufficient for `trusted`), and is now **ACCEPTED** as exactly that
negative boundary and R0 safety decision — acceptance does not close
Blocker B for mutation and does not authorize WAVE B1. The current
sequence is **WAVE B0 (ACCEPTED) → R0 (read-only runtime activation,
DONE — WAVE R0-A/R0-B reviewed and merged via PR #35, not blocked on
Blocker B) → operational activation on a real host (**GO**; >=24-hour
window COMPLETE, all five PR #47 observations passed on 2026-08-23) → a
future, separately reviewed and accepted stronger
continuity-proof ADR (not started) → WAVE B1 (deferred until that ADR
exists)**; mutation/Phase 1C work remains separate and gated on its own
later review after that.

## CONTRACT-ONLY

The accepted architecture defines the following concepts, and Phase 0 publishes/validates enough shape for HA presentation, but their durable backend owners are not implemented yet:

- durable node bindings/attestations (ADR 0001; distinct from the now-
  implemented source-level attestation authority in WAVE C1 above);
- source endpoint activation procedure (candidate/inactive -> active,
  active-endpoint replacement, automatic/operator failover -- ADR 0003 §15;
  candidate *attestation* itself is durably implemented, activation is not);
- workload/resource continuity proof (ADR 0001 "Blocker B");
- stored policy revisions and effective-capability computation;
- maintenance approvals/plans/jobs/locks/audit;
- typed mutation authorization against current identity/binding/revisions;
- durable operation/restart reconciliation;
- final 0.5 break-glass/self-host recovery binding.

Do not treat the existence of Phase 0 fields/validators as proof that these backend mechanisms exist.

## NOT IMPLEMENTED

The following are not implemented as production 0.5 features. Note: the
merged WAVE R0-B read-only runtime (below, and "COMPLETE") already
implements the concrete production PVE transport, R0-owned discovery
scheduling, and the published snapshot/HTTP API wiring — those items
have been removed from this list rather than left here stale; everything
remaining below is genuinely not implemented, and none of it is
authorized to begin until its own gate closes (Blocker B, a future
mutation-authority ADR, etc.):

- endpoint activation/source-binding UI/API backed by durable 0.5 state
  (source *creation*/bootstrap itself is implemented — see "COMPLETE" —
  this item is specifically about activating a candidate/replacement
  endpoint, which remains unauthorized per ADR 0003 §15);
- enrollment/trust workflow;
- resource selection/management policy workflow;
- 0.5 policy/plans/jobs/locks/audit and mutation authority;
- production effective-capability engine;
- maintenance/update planning and execution using the 0.5 identity model;
- snapshot/rollback/restore flows bound to the 0.5 resource/binding/revision model;
- restart/crash reconciliation for 0.5 jobs;
- native HA mutation controls for the 0.5 backend path;
- production 0.5 self-update;
- final 0.5 break-glass design;
- independent destructive end-to-end validation of the complete 0.5 mutation path;
- 0.5 public release.

The existing stable 0.4 runtime/recovery mechanisms remain legacy behavior and must not be mistaken for implementation of the clean-break 0.5 contracts above.

## DEFERRED / PHASE 1+

These guarantees require backend/SQLite durable ownership and must not be recreated opportunistically in Home Assistant:

- source endpoint activation/failover proof (source-level attestation proof
  itself is now durably implemented by WAVE C1 above; activation/failover
  remain deferred pending their own future ADR, ADR 0003 §15);
- persistent trust/enrollment history (source-level attestation history is
  now durably retained by WAVE C1; node-level enrollment/trust history --
  ADR 0001's node attestation -- remains unimplemented; workload/
  resource-level continuity proof, "Blocker B", has no authorized
  implementation path at all under the current stock-PVE baseline per
  ADR 0005's corrected conclusion, and awaits a future stronger-proof ADR
  before any implementation can be authorized);
- policy/approval/job/lock authority;
- mutation execution and reconciliation after process/host restart;
- final break-glass security model.

A Phase 0 validation proposal that needs any of these is a Phase 1/backend requirement, not a reason to add hidden authority or durable history to HA.

## KNOWN NON-BLOCKERS / ENGINEERING DEBT

### Contract test strategy

The deterministic A-H matrix and Hypothesis P1-P16 property suite are merged and
closed as the Phase 0 regression/oracle baseline. Re-open a bounded family only
for a concrete reachable counterexample.

### Native Windows Home Assistant harness

Exact HA integration pytest is a Linux CI/devcontainer/WSL-class gate, not native Windows. The `fcntl` import limitation is known and should not be "fixed" in repository code.

### HA presentation polish (P3-UX-1, P3-UX-2)

Two non-blocking Home Assistant presentation items remain open, observed during
the historical PR #43 operational checkpoint:

- **P3-UX-1** — HA timestamps currently render as raw ISO values; later
  HA-native timestamp presentation polish is open work.
- **P3-UX-2** — a resource's "Node" sensor currently exposes the opaque
  backend node UUID rather than a human-readable current node name; identity
  must remain backend-owned, so any fix is presentation-only.

Neither blocks anything and neither changes any accepted contract. See
`docs/archive/project-history/0.5-r0-activation-chronology.md` for the
detailed historical evidence.


## Canonical stage sequence — current status

| Stage | Status |
| --- | --- |
| C0/C1 (source-binding/attestation, "Blocker C") | **DONE** — architecture ACCEPTED (ADR 0003), durable authority implemented and dormant (PR #30, `fee55abdfbeea4597efaa780db06ad6ed4cc4ccb`) |
| A0/A1 (confirmed removal, "Blocker A") | **DONE** — architecture ACCEPTED (ADR 0004), durable authority implemented and dormant (PR #32, `ca94e03da768a1038533ac24753bd0820633864b`) |
| B0 (workload continuity, "Blocker B" architecture) | **ACCEPTED** (ADR 0005) — negative stock-PVE trust boundary / R0 safety decision only |
| R0-A (read-only runtime activation design) | **DONE** — `docs/architecture/0.5-r0-read-only-runtime-activation.md` |
| R0-B (read-only runtime implementation) | **DONE, MERGED** — PR #35, merge commit `20c0484d1efffcf1fe700ac1cd232cecba4fc0ea` |
| R0-C (automated Proxmox bootstrap) | **DONE, MERGED** — PR #38 plus corrective passes; dogfood #3 achieved a fresh Phase 1-13 PASS on a real host |
| R0 operational activation (real host + Home Assistant) | **GO** — bootstrap PASS, HA enrollment/acceptance PASS, >=24-hour observation COMPLETE, all five PR #47 observations PASS on the 2026-08-23 re-check |
| ADR 0006 (Blocker B, negative/unresolved research record) | **ACCEPTED** — selects no sufficient mechanism, changes no classification |
| Family B / B-S1 (task-history witness mechanism research) | **ABANDONED as an implementation path** — NO-GO; see `docs/archive/postmortems/blocker-b-family-b-13.md` |
| future positive Blocker-B mechanism ADR | **NOT STARTED / UNRESOLVED** — a *different*, later ADR that actually defines a sufficient mechanism; none has been proposed |
| B1 (durable trusted-enrollment implementation) | **DEFERRED / NOT AUTHORIZED**, gated on that future positive ADR being separately reviewed and ACCEPTED |
| Phase 1C (policy/jobs/mutation authority) | **BLOCKED** until Blocker B and every other mutation gate closes |

## Current R0 operational status — GO

The operator performed the re-check scheduled by PR #47 on 2026-08-23 and
accepted all five previously-open operational observations: disposable new
LXC discovery, rename/identity preservation, synthetic PVE API outage and
recovery, forced freshness expiry and recovery, and abnormal-stop stranded-run
startup fencing — all **PASS**, with concrete witnesses recorded in
`docs/operations/0.5-r0-operational-activation.md` §13 (the exact per-observation
witness table is also preserved in
`docs/archive/project-history/0.5-r0-activation-chronology.md`).

| Item | Current result |
| --- | --- |
| R0 bootstrap | **PASS** |
| Home Assistant enrollment / acceptance | **PASS** |
| recommended >=24-hour operational observation | **COMPLETE** |
| five PR #47 operational observations | **PASS — zero remain open** |
| node migration | **N/A** — single-node topology |
| R0 operational decision | **GO** |
| R0 mode | **STRICTLY READ-ONLY** |
| mutation authority | **NONE** |

This operational GO closes only the R0 operational-activation observation gate.
It does not change ADR 0005 or ADR 0006, select a stronger-proof mechanism,
prove workload security continuity, or authorize mutation. Blocker B remains
**OPEN**; WAVE B1 remains **DEFERRED / NOT AUTHORIZED**; Phase 1C remains
**BLOCKED**; mutation authority remains **NONE**; R0 remains **READ-ONLY**;
resources receive no transition to `security_continuity=trusted`; and
destructive policy/capabilities remain false, unavailable, or none as
applicable.

## Active next product direction

The operator's current product target is recorded in `docs/product-intent.md`.
In short: static 0.4.x → PVE autodiscovery → dynamic backend inventory →
dynamic Home Assistant resources → **safe, operator-approved** update workflow,
with **NO AUTO-UPDATE** as a hard product rule.

Against that target, the implemented state above delivers autodiscovery,
dynamic backend inventory, and dynamic Home Assistant representation. The
remaining product surface — read-only package/update scanning and presentation,
operator approval of an update plan, job-owned pre-update snapshots, health
checking, and same-job rollback — is **not implemented and not yet authorized to
begin**. Each of those pieces needs its own accepted architecture before any
implementation package exists:

1. **Read-only package/update scanning and presentation.** No ADR covers it yet.
   It is read-only by nature and therefore the closest item to the existing
   accepted discovery model, but the transport, cadence, staleness, and snapshot
   contract for package facts are undesigned. Note that the current R0 PVE
   transport is GET-only against the PVE API and has no guest-agent, SSH, or
   in-guest execution path at all — package scanning needs a new, separately
   designed and reviewed evidence channel.
2. **Operator-approved update plans, jobs, snapshots, health checks, and
   same-job rollback.** This is Phase 1C. It remains **BLOCKED** under accepted
   architecture (ADR 0005 §26, ADR 0006). The operator has stated that the
   unresolved persistent-incarnation question should not keep blocking a
   *same-job*, approval-scoped update/snapshot/rollback workflow. Whether that
   narrower mutation path can be authorized without first closing Blocker B is a
   **NEW ARCHITECTURAL DECISION** requiring its own ADR and its own review; it
   is not decided by this document, and nothing here relaxes the current
   BLOCKED status.

What is explicitly *not* the next direction: reviving Family B / B-S1, executing
Experiment #13, or continuing the PR #52 / PR #53 research branches.

## Family B / B-S1 — not the current path

Family B / B-S1 was an attempt to prove workload continuity through complete
PVE task/history observation. Two unmerged research branches (PR #52, PR #53)
built ~25,700 lines of harness/analyzer apparatus and Experiment #13 was never
executed. The architecture recovery review returned **NO-GO** on B-S1 as the
mutation-authority path and **NO-GO** on continuing PR #53's architecture.

- The concise durable record is `docs/archive/postmortems/blocker-b-family-b-13.md`.
- The superseded research documents are in `docs/archive/blocker-b-family-b/`.
- Blocker B itself remains a distinct, **OPEN**, unresolved question. It is not
  closed, and it was not solved by this research.

Ordinary product work must not read the archived Family-B material and must not
treat it as a roadmap.

## Where the history lives

- `docs/archive/project-history/0.5-r0-activation-chronology.md` — the verbatim
  acceptance/review/dogfood narrative formerly inlined in this document.
- `docs/archive/postmortems/blocker-b-family-b-13.md` — the PR #52/#53 postmortem.
- `docs/archive/blocker-b-family-b/` — the superseded Family-B research documents.
- `docs/operations/0.5-r0-operational-activation.md` §11–§13 — the operator-side
  activation checkpoints and their evidence.
- Git history and PR bodies remain the archive for everything else.
